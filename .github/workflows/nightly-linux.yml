name: Linux Build

on:  
  workflow_dispatch:
  
jobs:
  build:
    name: Build on Linux
    runs-on: ubuntu-latest
    steps:

      - name: Install tools
        run: |
          sudo apt-get install curl jq -y

      - name: Obtain last commit
        run: |
          echo $(curl -L -H "Accept: application/vnd.github+json" -H "X-GitHub-Api-Version: 2022-11-28" https://api.github.com/repos/cosmos/gaia/branches/release/v9.0.x | jq -r '.commit.sha')

      # - name: Setup Environment
      #   run: |
      #     sudo apt install build-essential curl jq wget -y
      # - name: Install golang
      #   run: |
      #     wget https://go.dev/dl/go1.18.5.linux-amd64.tar.gz
      #     sudo tar -C /usr/local -xzf go1.18.5.linux-amd64.tar.gz

      # - name: Clone and Build Gaia
      #   run: |
      #     export PATH=$PATH:/usr/local/go/bin
      #     git clone https://github.com/cosmos/gaia.git
      #     cd gaia
      #     git checkout release/v9.0.x
      #     make build

      # - name: Upload Assets
      #   uses: actions/upload-artifact@v3
      #   with:
      #     name: Gaia-Linux
      #     path: ./gaia/build/


