- name: Run gaia
  hosts:
    localhost
  connection: local
  tasks:
    - name: get gaia version
      become_user: gaia
      shell: |
        whoami
        pwd
        ls ~/.gaiad
        gaiad version

