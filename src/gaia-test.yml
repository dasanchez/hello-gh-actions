- name: Run gaia
  hosts:
    localhost
  connection: local
  become: true
  tasks:
    - name: get gaia version
      become_user: gaia
      shell: |
        whoami
        pwd
        ls ~/.gaiad
        gaiad version
      environment:
        PATH: ~/.gaiad/cosmovisor/current/bin:{{ ansible_env.PATH }}